{% extends 'base.html.twig' %}

{% block title %}Grade{% endblock %}

{% block body %}
    <h1>Grade</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grade.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ grade.nom }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ grade.montant }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ grade.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backend_grade_index') }}">back to list</a>

    <a href="{{ path('app_backend_grade_edit', {'id': grade.id}) }}">edit</a>

    {{ include('backend_grade/_delete_form.html.twig') }}
{% endblock %}
