{% extends 'base.html.twig' %}

{% block title %}Cloture{% endblock %}

{% block body %}
    <h1>Cloture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cloture.id }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ cloture.statut ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backend_cloture_index') }}">back to list</a>

    <a href="{{ path('app_backend_cloture_edit', {'id': cloture.id}) }}">edit</a>

    {{ include('backend_cloture/_delete_form.html.twig') }}
{% endblock %}
