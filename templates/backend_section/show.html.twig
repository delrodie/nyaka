{% extends 'base.html.twig' %}

{% block title %}Section{% endblock %}

{% block body %}
    <h1>Section</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ section.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ section.code }}</td>
            </tr>
            <tr>
                <th>Paroisse</th>
                <td>{{ section.paroisse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backend_section_index') }}">back to list</a>

    <a href="{{ path('app_backend_section_edit', {'id': section.id}) }}">edit</a>

    {{ include('backend_section/_delete_form.html.twig') }}
{% endblock %}
