<form method="post" action="{{ path('app_backend_vicariat_delete', {'id': vicariat.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce vicariat?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vicariat.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
